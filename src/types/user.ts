export type AuthUserType = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};

export type ProfileType = {
  firstName: string;
  lastName: string;
  email: string;
};
